import styled from 'styled-components';

// Shared component just extracted to not repeat from file to file
const Content = styled.div`
  width: 100%;
  min-height:100%;
  max-width: 1360px;
  margin: auto;
  display: flex;
`;

export default Content;
